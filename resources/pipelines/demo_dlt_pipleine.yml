resources:
  pipelines:
    pipeline_demo_dlt_pipelines:
      name: Demo_DLT_Pipelines
      libraries:
        - glob:
            include: ../notebooks/demo_Pipeline.sql
      schema: ${resources.schemas.bronze_schema.name}
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${resources.schemas.bronze_schema.catalog_name}
      serverless: true
      root_path: /Workspace/Users/amar.kaliaperumal@gmail.com/Demo_DLT_Pipelines
